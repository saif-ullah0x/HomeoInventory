-- HomeoInvent Database Schema
-- Replace this with your actual schema from Neon dashboard

CREATE TABLE medicines (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  -- Add other columns here
);

CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  -- Add other columns here
);

-- Add other tables as needed
